services:
  tradebot:
    build: .
    container_name: tradebot
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./tradebot.db:/app/tradebot.db
    env_file:
      - .env
    restart: unless-stopped